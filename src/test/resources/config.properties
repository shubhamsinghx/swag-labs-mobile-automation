# ============================================================
# Swag Labs Mobile Automation — Local Configuration
# ============================================================

# Execution Platform: local | lambdatest | browserstack
execution.platform=local

# Appium Server
appium.server.url=http://127.0.0.1:4723

# App Configuration
app.path=app/Android.SauceLabs.Mobile.Sample.app.2.7.1.apk
app.package=com.swaglabsmobileapp
app.activity=com.swaglabsmobileapp.MainActivity

# Device Configuration
platform.name=Android
device.name=Android Emulator
platform.version=16
automation.name=UiAutomator2

# App Reset
no.reset=false
full.reset=false

# Timeouts (in seconds)
implicit.wait.seconds=10
explicit.wait.seconds=15
new.command.timeout=300

# Retry Configuration
max.retry.count=2
